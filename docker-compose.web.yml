services:
  leoland-web:
    image: nginx:1.27-alpine
    container_name: leoland-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:39510:80"
    volumes:
      - ./app/dist:/usr/share/nginx/html:ro
      - ./ops/web/nginx.conf:/etc/nginx/conf.d/default.conf:ro
